A single transaction in a list.

```jsx
<TransactionRow icon={<GamepadIcon/>} title="Gamestop" category="Shopping" amount="$456.0" time="09:18 AM" />
```

Use `dark` inside the striped forest list (Transactions screen); omit for paper surfaces.
