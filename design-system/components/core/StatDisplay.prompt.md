The big balance/total readout at the top of a screen.

```jsx
<StatDisplay label="Total Spent" amount="$1,894.0" />
<StatDisplay label="Available June Budget" amount="$1,894.0" />
```

The decimal tail (".0") is automatically dimmed to --ink-faint — pass the full string.
